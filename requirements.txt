# 핵심
pdfplumber>=0.10.0
rank-bm25>=0.2.2
sentence-transformers>=2.7.0
faiss-cpu>=1.7.4
numpy>=1.24.0
tqdm>=4.66.0

# API 서버
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.5.0

# LLM (외부 API 미사용 — 호환성 위해 의존성 제거. 로컬 GGUF/Korean ST만 사용)

# 로컬 .env 로드 (Windows/Linux 공통)
python-dotenv>=1.0.0

# (선택) 더 정확한 한국어 토크나이즈를 원하면 주석 해제
# konlpy>=0.6.0
